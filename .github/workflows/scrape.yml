# name: Weekly Job Scrape

# on:
#   schedule:
#     - cron: '0 9 * * 1'    # Every Monday 09:00 UTC
#   workflow_dispatch:          # Manual trigger from GitHub Actions tab

# jobs:
#   scrape:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write          # Needed to commit back to repo

#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v4

#       - name: Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'
#           cache: 'pip'
#           cache-dependency-path: scraper/requirements.txt

#       - name: Install dependencies
#         run: pip install -r scraper/requirements.txt

#       - name: Scrape all sources
#         run: python scraper/scrape_jobs.py
#         # Add --limit 500 to cap per-source if you want smaller runs

#       - name: Aggregate into dashboard JSON
#         run: python scraper/aggregate_free.py

#       - name: Commit updated data
#         run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "github-actions[bot]@users.noreply.github.com"
#           git add data/
#           git diff --staged --quiet || git commit -m "data: weekly scrape $(date +%Y-%m-%d)"
#           git push

#       # Vercel will auto-redeploy when data changes are pushed
#       # No extra steps needed if you have Vercel connected to this repo
